package main

import (
	"fmt"
)

func main() {
	str := "Go 语言😂" // 包含英文、中文、emoji

	// 1. 按 byte 遍历（for 循环）：拆成单个字节
	fmt.Println("按 byte 遍历（长度=", len(str), "）：")
	for i := 0; i < len(str); i++ {
		fmt.Printf("%d: %v (十六进制: %x)\n", i, str[i], str[i])
	}
	// 输出长度是 11：因为 "Go"（2 字节）+ "  "（1 字节空格）+ "语言"（每个中文 3 字节，共 6）+ "😂"（4 字节）= 2+1+6+4=13？不对，实际 len(str) 是 13？
	// 哦，实际计算："Go"是 2 字节，空格 1 字节，"语"是 3 字节（UTF-8 编码），"言"是 3 字节，"😂"是 4 字节 → 2+1+3+3+4=13，所以 len(str) 是 13

	fmt.Println("\n按 rune 遍历（for range）：当作整体")
	for i, char := range str {
		fmt.Printf("索引 %d: 字符=%c, 类型=rune, 码点=%U\n", i, char, char)
	}
	fmt.Printf("%c", []rune(str)[4])
	// 输出 5 个元素：'G'、'o'、' '、'语'、'言'、'😂' → 共 6 个 rune，因为每个字符是一个整体
}
